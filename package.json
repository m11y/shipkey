{
  "name": "shipkey",
  "version": "0.3.6",
  "type": "module",
  "workspaces": ["packages/*"],
  "repository": {
    "type": "git",
    "url": "https://github.com/chekusu/shipkey"
  },
  "bin": {
    "shipkey": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "compile": "bun build src/index.ts --compile --outfile shipkey",
    "test": "bun test"
  },
  "dependencies": {
    "@shipkey/core": "workspace:^",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
