{
  "name": "shipkey",
  "version": "0.3.16",
  "type": "module",
  "workspaces": ["packages/*"],
  "repository": {
    "type": "git",
    "url": "https://github.com/chekusu/shipkey"
  },
  "bin": {
    "shipkey": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node --define \"__GIT_COMMIT__='$(git rev-parse --short HEAD)'\"",
    "compile": "bun build src/index.ts --compile --outfile shipkey --define \"__GIT_COMMIT__='$(git rev-parse --short HEAD)'\"",
    "test": "bun test"
  },
  "dependencies": {
    "@shipkey/core": "^0.1.0",
    "commander": "^13.0.0",
    "dotenv": "^17.3.1",
    "dotenv-expand": "^12.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
